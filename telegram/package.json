{
  "name": "job4menow-telegram-bot",
  "version": "0.1.0",
  "description": "Continuously-hosted Telegram bot for reviewing job queues produced by career-ops (or any other producer), backed by Aurora PostgreSQL Serverless v2 (Data API) and S3.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/bot.mjs",
    "cli": "node src/cli.mjs",
    "test": "node --test test/"
  },
  "dependencies": {
    "@aws-sdk/client-rds-data": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "dotenv": "^16.4.5"
  }
}
