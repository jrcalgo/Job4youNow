"""The LangGraph supervisor — predictive tasks only. Deterministic Telegram
routes (routing/deterministic.py) never construct or invoke this graph; see
that module's docstring for why the split matters.
"""
