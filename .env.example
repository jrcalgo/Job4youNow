# job4meNow — copy to .env and fill in. Never commit the real file.

# Cursor SDK (agent/) — required even when the local `cursor-agent` CLI is
# already logged in (verified: local runtime does NOT reuse CLI session auth,
# it always needs an explicit key). Get one from:
# https://cursor.com/dashboard/integrations
CURSOR_API_KEY=
